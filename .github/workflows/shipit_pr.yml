name: Deploy Stencil Web components Library to NPM

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize # canary on new commit
 
jobs:
  stencil-library-release:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, 'ci skip') && !contains(github.event.head_commit.message, 'skip ci')" # job will not run, if triggered via ship-it
    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # fetch all tags for ship-it

      - name: download + setup auto
        uses: auto-it/setup-auto@v1

      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: 16
          registry-url: "https://registry.npmjs.org"

      - name: Create Release for stencil # releasing as npm package
        id: release-stencil
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

        run: | # here, auto shipit can be used, but after the first canary version has been created, the command fails to use it on the other packages under the same commit
          cd packages/components
          npm install
          npm run build:stencil
          auto shipit

      - name: Create Release for vue wrapper # releasing as npm package
        id: release-vue
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

        run: | # here, auto shipit can be used, but after the first canary version has been created, the command fails to use it on the other packages under the same commit
          cd ../components-vue
          npm install
          npm run build
          auto shipit

      - name: Create Release for react wrapper # releasing as npm package
        id: release-react
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

        run: | # here, auto shipit can be used, but after the first canary version has been created, the command fails to use it on the other packages under the same commit
          cd ../components-react
          npm install
          npm run build
          auto shipit
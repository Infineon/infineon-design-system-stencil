'use strict';

const componentsReact = require('..');
const assert = require('assert').strict;

assert.strictEqual(componentsReact(), 'Hello from componentsReact');
console.info('componentsReact tests passed');

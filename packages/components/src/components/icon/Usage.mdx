import { Meta } from '@storybook/addon-docs/blocks';

import * as IconStories from './icon.stories';
import IconButtonUsage from '../../storybook/assets/icon/icon-button-usage.png';
import IconDescription from '../../storybook/assets/icon/icon-description.png';
import IconInsideComponents from '../../storybook/assets/icon/icon-inside-components.png';
import IconUsage from '../../storybook/assets/icon/icon-usage.png';


<Meta of={IconStories} /> 


# Icon
Our UI icon library exists in two matching forms: a Figma library for design and the same icons in code for development.

| Resources |
|-----------|
| [Figma UI icon library →](https://www.figma.com/design/yWwaLoqsWLWygDxXfvdym9/Infineon-DDS-%7C-UI-icon-library?node-id=13284-1289) |

## The icon library as a component
In code you reach the library through the icon component — pick any icon by its name, with no individual assets to import or maintain. You can choose from our full UI icon library; browse all available icons in the [Icon Library](?path=/docs/icon-library--development). Because Figma and code hold the same icons under the same names, design and development always reference the same set, which keeps handover simple and avoids drift.

## Icon descriptions
We're adding a short, structured description to every icon. These are being introduced in Figma and will progressively become visible here in Storybook as well, to make choosing the right icon easier. Each description covers:

- **Metaphor** — the visual idea the icon communicates.
- **Use for** — the product contexts and actions where it fits.
- **Keywords** — search terms you might type to find it.
- **Avoid for** — cases where it could be confused with another icon, always pointing to the icon to use instead.
<img src={IconDescription} alt="Icon Description" />

## Icons inside components
Most of our components already embed the icon component wherever they need one — buttons, alerts, navigation, inputs and many more. Because they all build on the same icon component, icons look and behave consistently across the whole system and come correctly sized and aligned out of the box.

Where a component exposes its icon, you can swap or adjust it freely — flexibility without breaking the consistency the system provides.
<img src={IconInsideComponents} alt="Icon Inside Components" />


## Size
Icons have no fixed size — they scale dynamically to whatever size the surrounding context requires and stay crisp at any scale. Inside our components the size is handled for you. For visual balance we recommend sizing icons between 12×12px and 32×32px — a recommendation, not a hard limit.
<img src={IconSizes} alt="Icon Sizes" />


## When to use the icon component
Use the **icon component** to display an icon — for example next to text or as a visual marker in your own layout. On its own it just shows an icon and isn't interactive.
<img src={IconUsage} alt="Icon usage" width="50%" />

## When to use the icon button component
Use the **icon button** component when the icon should be clickable and trigger an action, such as close, edit, or delete. It turns an icon into an interactive button.
<img src={IconButtonUsage} alt="Icon Button Usage" width="50%" />


If you're already using a DDS component that includes an icon, rely on its built-in icon instead of adding one on top.

***
Looking for design guidance such as sizing relative to typography, vertical alignment and color? See **[Foundations / Iconography](?path=/docs/foundations-iconography--development)**.
// This file is loaded once per application that uses the components.
// This is configured via the "globalScript" property in the stencil.config.ts file.

export default function setup() {
	// Any global setup can be done here
}

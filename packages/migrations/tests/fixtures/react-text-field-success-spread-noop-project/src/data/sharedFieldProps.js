// Exported const object — success key must NOT be renamed by the migrator
// (the migrator cannot trace cross-file imports)
export const sharedFieldProps = {
	label: "imported object",
	success: true,
};
